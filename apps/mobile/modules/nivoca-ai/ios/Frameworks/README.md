# LiteRT-LM XCFramework (not checked in)

This directory is meant to contain `LiteRTLM.xcframework` (~205 MB), which
is gitignored because its two static-archive binaries are ~82 MB each.

If this directory is empty on your clone, follow the fetch + layout
instructions in [`_docs/mobile-setup.md`](../../../../../_docs/mobile-setup.md)
before running `bunx expo run:ios`.
